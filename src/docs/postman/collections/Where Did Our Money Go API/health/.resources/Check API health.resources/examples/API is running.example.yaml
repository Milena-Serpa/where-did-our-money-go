$kind: http-example
request:
  url: "{{baseUrl}}/health"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "status": "ok",
        "service": "where-did-our-money-go-api"
      }
order: 1000
