$kind: http-request
url: "{{baseUrl}}/auth/login"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "email": "milena@example.com",
      "password": "strong-password"
    }
examples: ./.resources/Login a user.resources/examples
order: 1000
